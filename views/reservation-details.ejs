<!DOCTYPE html>
<html lang="ru">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>TRIBOOK Reservation Details</title>

	<!-- Bootstrap CSS -->
	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

	<!-- Google Fonts -->
	<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap" rel="stylesheet">

	<!-- Font Awesome for icons -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />

	<!-- Custom CSS -->
	<link rel="stylesheet" href="/css/styles.css">
</head>

<body class="d-flex flex-column min-vh-100">
	<%- include('./partials/navbar') %>


		<div class="container mt-5 flex-grow-1">
			<h1 class="text-center text-primary mb-5 reservation-title">Детали Бронирования</h1>

			<div class="card reservation-card p-5 shadow-lg rounded-lg">
				<div class="mb-4">
					<h3 class="text-secondary">Email Клиента:</h3>
					<p class="fs-5">
						<i class="fas fa-envelope me-2 text-primary"></i>
						<a href="mailto:<%= reservation.email %>" class="text-decoration-none">
							<%= reservation.email %>
						</a>
					</p>
				</div>

				<div class="mb-4">
					<h3 class="text-secondary">Квартира:</h3>
					<% if (reservation.apartment) { %>
						<p class="fs-5 d-flex align-items-center">
							<i class="fas fa-home me-2 text-primary"></i>
							<span>
								<%= reservation.apartment.title %>
							</span>
							<a href="/apartment/<%= reservation.apartment._id %>"
								class="btn btn-info btn-sm ms-3">Посмотреть Квартиру</a>
						</p>
						<% } else { %>
							<p class="fs-5 text-muted"><em>Квартира не привязана</em></p>
							<% } %>
				</div>

				<div class="row mb-4">
					<div class="col-md-6 mb-3 mb-md-0">
						<h3 class="text-secondary">Дата Начала:</h3>
						<p class="fs-5">
							<i class="fas fa-calendar-alt me-2 text-primary"></i>
							<%= new Date(reservation.startDate).toLocaleDateString() %>
						</p>
					</div>
					<div class="col-md-6">
						<h3 class="text-secondary">Дата Окончания:</h3>
						<p class="fs-5">
							<i class="fas fa-calendar-alt me-2 text-primary"></i>
							<%= new Date(reservation.endDate).toLocaleDateString() %>
						</p>
					</div>
				</div>

				<div class="d-flex justify-content-between flex-column flex-md-row">
					<!-- Кнопка для связи с клиентом через email -->
					<a href="mailto:<%= reservation.email %>" class="btn btn-primary mb-3 mb-md-0">
						<i class="fas fa-envelope me-2"></i> Связаться с Клиентом
					</a>

					<!-- Кнопка для возврата к списку бронирований -->
					<a href="/admin/reservations" class="btn btn-secondary">
						<i class="fas fa-arrow-left me-2"></i> Назад к Бронированиям
					</a>
				</div>
			</div>
		</div>

		<%- include('./partials/footer') %>

			<!-- Bootstrap JS -->
			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

			<!-- Custom JS -->
			<script src="/js/main.js"></script>
</body>

</html>